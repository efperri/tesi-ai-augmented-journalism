# Fact-sheet JSON dagli articoli originali

Questa directory contiene i fact-sheet estratti dagli articoli in `data/original_articles/`.

Ogni file dovrebbe rispettare lo schema in `metadata/factsheet.schema.json` e mantenere un riferimento all'identificativo dell'articolo sorgente.
