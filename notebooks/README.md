# Notebook

Questa directory contiene notebook esplorativi o riproducibili per analisi di linguistica computazionale, confronto dei corpora e valutazioni etiche.

Quando possibile, ogni notebook dovrebbe indicare input usati, output prodotti, dipendenze, data dell'ultima esecuzione e relazione con risultati in `results/`.
